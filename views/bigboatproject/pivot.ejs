<% layout('layout') -%>
<div class="hero-unit">
  <h2>pivot</h2>
  <p>
    navigation is a drop down of pivot - category | year
    columns of each category (ordered lists) containing a list of all in the category (ordered list again)
    with a filter text box for finding items
  </p>
</div>
 